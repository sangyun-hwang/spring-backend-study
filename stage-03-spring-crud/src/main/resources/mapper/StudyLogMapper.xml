<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.stage03.mapper.StudyLogMapper">
    <resultMap id="studyLogResultMap" type="com.study.stage03.domain.StudyLog">
        <id property="id" column="id"/>
        <result property="title" column="title"/>
        <result property="category" column="category"/>
        <result property="minutes" column="minutes"/>
        <result property="memo" column="memo"/>
    </resultMap>

    <select id="search" resultMap="studyLogResultMap">
        SELECT id, title, category, minutes, memo
        FROM study_logs
        <where>
            <if test="category != null">
                category = #{category}
            </if>
            <if test="title != null and title != ''">
                AND title LIKE CONCAT('%', #{title}, '%')
            </if>
        </where>
    </select>

    <select id="findById" resultMap="studyLogResultMap">
        SELECT id, title, category, minutes, memo
        FROM study_logs
        WHERE id = #{id}
    </select>

    <select id="getNextId" resultType="long">
        SELECT COALESCE(MAX(id), 0) + 1
        FROM study_logs
    </select>

    <insert id="save">
        INSERT INTO study_logs (id, title, category, minutes, memo)
        VALUES (#{id}, #{title}, #{category}, #{minutes}, #{memo})
    </insert>

    <update id="updatePartial">
        UPDATE study_logs
        <set>
            <if test="request.title != null">
                title = #{request.title},
            </if>
            <if test="request.category != null">
                category = #{request.category},
            </if>
            <if test="request.minutes != null">
                minutes = #{request.minutes},
            </if>
            <if test="request.memo != null">
                memo = #{request.memo},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM study_logs
        WHERE id = #{id}
    </delete>
</mapper>

